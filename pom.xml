<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.0.7</version>
        <relativePath/>
    </parent>
    <groupId>com.gateway</groupId>
    <artifactId>payment-gateway-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>payment-gateway</name>

    <modules>
        <module>gateway-kernel</module>
        <module>gateway-merchants</module>
        <module>gateway-providers</module>
        <module>gateway-payments</module>
        <module>gateway-app</module>
    </modules>

    <properties>
        <java.version>25</java.version>
        <archunit.version>1.5.0</archunit.version>
        <testcontainers.version>1.21.4</testcontainers.version>
        <webhook-delivery.version>0.1.1</webhook-delivery.version>
        <ulid-creator.version>5.2.3</ulid-creator.version>
        <bucket4j.version>8.14.0</bucket4j.version>
        <logstash-encoder.version>8.1</logstash-encoder.version>
        <wiremock.version>3.13.0</wiremock.version>
        <json-schema-validator.version>1.5.6</json-schema-validator.version>
        <bcpkix.version>1.80</bcpkix.version>
        <spotless.version>3.10.3</spotless.version>
    </properties>

    <repositories>
        <!-- GitHub Packages requires a token even to read; local builds resolve from ~/.m2, CI uses a PAT secret. -->
        <repository>
            <id>github-webhook-delivery</id>
            <url>https://maven.pkg.github.com/leonardolermen/webhook-delivery</url>
        </repository>
    </repositories>

    <build>
        <plugins>
            <!--
              Wrapping and imports only, and only because no human keeps a column limit by hand across
              five modules.

              google-java-format and not palantir: palantir wraps at 120 but indents with four spaces,
              and this repository has been two spaces since the first commit. Indentation is what you
              read on every line; the column limit is what you hit on a few. So the indentation won and
              the limit followed the tool, from 120 to 100 (CLAUDE.md and .editorconfig say 100).

              NOT bound to a phase: what a formatter cannot do is the part of the standard that matters
              (a name that says what the thing is, a blank line between two ideas), so `verify` failing
              on a column would say the wrong thing about what this repository cares about. Run
              `./mvnw spotless:apply` before committing, or `spotless:check` to see what it would touch.
            -->
            <plugin>
                <groupId>com.diffplug.spotless</groupId>
                <artifactId>spotless-maven-plugin</artifactId>
                <version>${spotless.version}</version>
                <configuration>
                    <java>
                        <includes>
                            <include>src/main/java/**/*.java</include>
                            <include>src/test/java/**/*.java</include>
                        </includes>
                        <excludes>
                            <!--
                              google-java-format's parser cannot read `case Outcome.InProgress() ->`: a record
                              pattern with no components inside a switch rule. It fails with "')' expected"
                              rather than formatting it. The code is valid Java 21 and the switch is the
                              clearest way to write that dispatch, so the file stays and the tool steps around
                              it. Format it by hand, to the same 100 columns.
                            -->
                            <exclude>src/main/java/com/gateway/app/api/support/IdempotencyFilter.java</exclude>
                        </excludes>
                        <googleJavaFormat/>
                    </java>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.testcontainers</groupId>
                <artifactId>testcontainers-bom</artifactId>
                <version>${testcontainers.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency><groupId>com.gateway</groupId><artifactId>gateway-kernel</artifactId><version>${project.version}</version></dependency>
            <dependency><groupId>com.gateway</groupId><artifactId>gateway-merchants</artifactId><version>${project.version}</version></dependency>
            <dependency><groupId>com.gateway</groupId><artifactId>gateway-providers</artifactId><version>${project.version}</version></dependency>
            <dependency><groupId>com.gateway</groupId><artifactId>gateway-providers</artifactId><version>${project.version}</version><type>test-jar</type></dependency>
            <dependency><groupId>com.gateway</groupId><artifactId>gateway-payments</artifactId><version>${project.version}</version></dependency>
            <dependency><groupId>com.barrier</groupId><artifactId>webhook-delivery</artifactId><version>${webhook-delivery.version}</version></dependency>
            <dependency><groupId>org.wiremock</groupId><artifactId>wiremock-standalone</artifactId><version>${wiremock.version}</version></dependency>
            <dependency><groupId>com.networknt</groupId><artifactId>json-schema-validator</artifactId><version>${json-schema-validator.version}</version></dependency>
            <dependency><groupId>org.bouncycastle</groupId><artifactId>bcpkix-jdk18on</artifactId><version>${bcpkix.version}</version></dependency>
            <dependency><groupId>com.github.f4b6a3</groupId><artifactId>ulid-creator</artifactId><version>${ulid-creator.version}</version></dependency>
            <dependency><groupId>com.bucket4j</groupId><artifactId>bucket4j_jdk17-core</artifactId><version>${bucket4j.version}</version></dependency>
            <dependency><groupId>net.logstash.logback</groupId><artifactId>logstash-logback-encoder</artifactId><version>${logstash-encoder.version}</version></dependency>
            <dependency><groupId>com.tngtech.archunit</groupId><artifactId>archunit-junit5</artifactId><version>${archunit.version}</version><scope>test</scope></dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency><groupId>org.junit.jupiter</groupId><artifactId>junit-jupiter</artifactId><scope>test</scope></dependency>
        <dependency><groupId>org.assertj</groupId><artifactId>assertj-core</artifactId><scope>test</scope></dependency>
    </dependencies>
</project>
